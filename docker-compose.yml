version: "3.8"
services: 
  program:
    build: .
    container_name: program
    environment: 
    - NODE_ENV=development
    - CHOKIDAR_USEPOLLING=true
    - MEMORY_LIMIT=8192
    env_file:
    - .env
    volumes: 
    - ./:/usr/src/app
    ports:
    - 8080:8080
    stdin_open: true
    tty: true
    command: "/usr/src/app/entrypoint.sh"